#include <iostream>
#include <stdlib.h>
#include <windows.h>
#include <fstream>
#include <ctime>
using namespace std;

int random(int);
bool k_ones_in_n(unsigned, unsigned);
double gravity(double, double, double);
void hanoi(int, char, char, char);
int pow(int, int);
double fact(double);
unsigned digits(unsigned);
unsigned pos_calc(unsigned, unsigned);
bool prime(unsigned);
void triangle(unsigned);
void spaces(unsigned, unsigned);
void draw(unsigned);
void save_text();
void load_text();
void set_scrn();
void welcome();